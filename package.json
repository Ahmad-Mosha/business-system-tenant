{
  "name": "prime-market",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run start:dev -w @prime/api",
    "db:up": "docker compose up -d",
    "test": "npm run test -w @prime/api",
    "typecheck": "npm run typecheck -w @prime/api"
  }
}